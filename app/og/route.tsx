export const runtime = 'edge'

export async function GET() {
  // Placeholder OG route; swap to @vercel/og later.
  return new Response('OG image placeholder', {
    headers: { 'content-type': 'text/plain' },
  })
}

